#include <cstdarg>
#include "std_dev.cc"

//Calculates the standard deviation of integers
// param int n = the number of integers to find deviation of
// param ... integers to find the deviation of 
double deviation(int n, ...);